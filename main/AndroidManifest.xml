<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.dloza.descendent">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SlashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dolores.LoginActivity"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".GalleryDetail" />
        <activity android:name=".ChurchActivity" />
        <activity android:name=".ImageSight" />
        <activity android:name=".ChurchDetail" />
        <activity android:name=".RestaurantActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".VideoDoloresActivity" />
        <activity android:name=".SubMenuVideoActivity" />
        <activity android:name=".VideoTwoDoloresActivity" />
        <activity android:name=".VideoThreeDoloresActivity" />
        <activity android:name=".VideoFourDoloresActivity" />
        <activity android:name=".DoloresGalleryActivity" />
        <activity android:name=".dolores.FirstActivity" />
        <activity android:name=".dolores.PrincipalActivity" />
        <activity android:name=".dolores.SnowDetail" />
        <activity android:name=".dolores.ToursActivity" />
        <activity android:name=".dolores.PaitingTour" />
        <activity android:name=".dolores.Register" />
        <activity android:name=".dolores.PhotoTour" />
        <activity android:name=".dolores.ModelTour" />
        <activity android:name=".dolores.StatueTour" />
        <activity android:name=".HandicraftsActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".hidalgo.CostTicketsActivity" />
        <activity android:name=".hidalgo.GameBottleActivity" />
        <activity android:name=".hidalgo.GuideAudioActivity" />
        <activity android:name=".hidalgo.LanguageActivity" />
        <activity android:name=".hidalgo.ShareActivity" />
        <activity android:name=".CreditActivity" />
        <activity android:name=".PuzzleActivity" />
        <activity android:name=".VideoFiveActivity" />
        <activity android:name=".hidalgo.CatGameActivity"></activity>
    </application>

</manifest>